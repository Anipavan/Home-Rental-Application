server:
  port: 8082

spring:
  application:
    name: maintenance-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/HomeRentalDB

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
