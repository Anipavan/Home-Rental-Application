# Local-dev secrets. See auth-service/application-dev.yaml for the
# rationale.
spring:
  datasource:
    password: Pavan@123

app:
  internal-auth:
    secret: CHANGE_ME_LOCAL_DEV_INTERNAL_AUTH_SECRET_PLACEHOLDER_AT_LEAST_32_BYTES_LONG
  # Mock gateway in dev — real Cashfree creds only land in prod.
  payment:
    gateway: mock
  cashfree:
    app-id: CHANGE_ME_CASHFREE_APP_ID_PLACEHOLDER
    secret-key: CHANGE_ME_CASHFREE_SECRET_KEY_PLACEHOLDER
    webhook-secret: CHANGE_ME_CASHFREE_WEBHOOK_SECRET_PLACEHOLDER
    environment: sandbox
