package com.spa.home_rental_application.payment_service.payment_service.enums;

/** UPI app the tenant is paying through. Sent only when method=UPI. */
public enum UpiApp {
    GPAY,
    PHONEPE,
    PAYTM,
    BHIM,
    AMAZONPAY,
    CRED,
    WHATSAPP,
    OTHER
}
